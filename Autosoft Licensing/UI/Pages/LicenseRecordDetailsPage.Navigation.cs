using System;

namespace Autosoft_Licensing.UI.Pages
{
    partial class LicenseRecordDetailsPage
    {
        /// <summary>
        /// Navigate back to LicenseRecordsPage.
        /// This method should be called by the host/MainForm to handle navigation.
        /// </summary>
        public void NavigateBack()
        {
            // TODO: Implement navigation via host/MainForm
            // For example:
            // var mainForm = this.FindForm() as MainForm;
            // if (mainForm != null)
            // {
            //     mainForm.ShowPage(new LicenseRecordsPage());
            // }
        }
    }
}